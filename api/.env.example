DB_USER=jonasapi
DB_PASSWORD=change-me

# Listmonk-Anbindung fuer Newsletter (optional – ohne diese Werte
# werden Abos nur in der DB gespeichert, aber nicht an Listmonk gemeldet)
LISTMONK_URL=https://liste.jonasstreule.ch
LISTMONK_API_USER=backend_api
LISTMONK_API_TOKEN=change-me
# Mapping campaign-slug -> Listmonk-Listen-ID
LISTMONK_LISTS={"hofladen-schutz":3}
